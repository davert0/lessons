arr[6] - arr[-1]
// обращение к последнему элементу массива заменено на более безопасное

arr.pop() - stack.pop()
// замена массива на стэк для получения последнего элемента

["field", 123, source-field: {}] - ("field", 123, source-field: {})
// замена массива на кортеж

EXCEPTIONS = ['foo', 'bar'] - exceptions = {'foo', 'bar'}
// замена массива на множество

arr.insert(0, itm) - queue.enque(itm)
// замена массива на очередь